Pet:
  type: object
  required: [id, name]
  properties:
    id:
      type: integer
    name:
      type: string
    tag:
      $ref: "#/Tag"

Tag:
  type: string

components:
  parameters:
    PetId:
      name: petId
      in: path
      required: true
      schema:
        type: integer
